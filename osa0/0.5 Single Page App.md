```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: After clicking save button JavaScript code parses user input to JSON format and adds new note to currently rendered html DOM structure.
    
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa with payload in JSON format (note and date)
    activate server
    server-->>browser: HTTP status code 201
    deactivate server

    Note right of browser: JSON data is sent with description of content type so that server know what it is recieving.

